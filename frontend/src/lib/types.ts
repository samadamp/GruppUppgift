export type eventCard = {
    id: number,
    title: string,
    desc: string,
    img: string,
    isPrivate: boolean,
};

export type eventDetails = {
    id: number,
    title: string,
    desc: string,
    img: string,
    location: string,
    time: string,
    max: number,
    private: boolean,
};