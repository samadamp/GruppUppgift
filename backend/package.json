{
  "name": "backend",
	"version": "1.0.0",
	"description": "Ett tillhörande REST-api för att rösta och se resultaten.",
	"main": "index.js",
	"scripts": {
		"dev": "npm run build && npm run start",
		"start": "node ./server/dist/index.js",
		"build": "npx esbuild server/index.ts --platform=node --bundle --outdir=./dist --format=cjs"
	},
  "dependencies": {
    "@types/express": "^5.0.0",
    "@types/mongoose": "^5.11.97",
    "@types/node": "^22.7.9",
    "express": "^4.21.1",
    "mongoose": "^8.7.2",
    "node": "^22.10.0"
  }
}
